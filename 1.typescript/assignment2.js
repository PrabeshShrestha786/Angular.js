function print(name, occupation, wage) {
    return `Hey, ${name}. You are an ${occupation} and your hourly wage is ${wage} dollars.`;
}

// Test the function
console.log(print("Prabesh", "Engineer", 25));
